#include <stdio.h>
int sum(int *a,int *b)
{
	return *a+*b;
}
main()
{
	int a,b,c,d;
	printf("Input 1st Number:");
	scanf("%d",&a);
	printf("Input 2nd Number:");
	scanf("%d",&b);
	printf("The Sum of %d and %d is %d",a,b,sum(&a,&b));
}
