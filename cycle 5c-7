#include <stdio.h>
int reverse(int a)
{
	int b=0,c,d;
	while(a!=0)
	{
		b=b*10+a%10;
		a/=10;
	}
	return b;
}
main()
{
	int a,b,c,d;
	printf("Enter The Number To reverse:");
	scanf("%d",&a);
	printf("The Reversed Number is %d",reverse(a));
}
